# Quickstart

```bash
npm install
npm run build
./mapper
```

A chromeless window opens showing the map. Drag to pan, use the wheel to
zoom, right-drag to rotate and tilt. Click the compass control in the
top-right to return to north-up and flat.
