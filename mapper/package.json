{
  "name": "mapper",
  "version": "0.1.0",
  "private": true,
  "description": "A chromeless desktop OpenStreetMap window",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node build.js",
    "start": "./mapper",
    "test": "node --test test/*.test.js && playwright test"
  },
  "dependencies": {
    "@preact/signals": "^1.3.4",
    "esbuild": "^0.24.0",
    "maplibre-gl": "^5.0.0",
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
